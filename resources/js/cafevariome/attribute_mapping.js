$(document).ready(function() {
    // If the table exists, populate DataTable and make it stylish.
    if ($('#attributeMappingsTable').length) {
        $('#attributeMappingsTable').DataTable();
    }
});
